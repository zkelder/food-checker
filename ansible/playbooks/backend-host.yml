---
- name: Configure Food Checker backend host
  hosts: backend
  become: true
  roles:
    - common
    - docker
    - aws_cli
    - ssm_agent
    - app_host
    - role: caddy
      when: caddy_verify | bool
